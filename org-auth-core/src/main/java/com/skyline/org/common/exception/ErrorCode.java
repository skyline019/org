package com.skyline.org.common.exception;

public enum ErrorCode {
    VALIDATION_ERROR,
    USERNAME_TAKEN,
    EMAIL_TAKEN,
    USER_NOT_FOUND,
    TOKEN_INVALID,
    TOKEN_EXPIRED,
    TOKEN_USED,
    RATE_LIMIT_EXCEEDED,
    INTERNAL_ERROR
}
