# 源码恢复脚本（历史归档）

P2 多模块迁移期间 `src/` 误删时，曾用这些 PowerShell 脚本从 JaCoCo HTML 报告批量恢复测试类。

**当前仓库已恢复正常源码，请勿再运行这些脚本。** 保留仅供审计参考。

| 文件 | 说明 |
|------|------|
| `create-tests.ps1` ~ `create-tests4.ps1` | 分批次生成 `org-app` 测试类骨架 |
